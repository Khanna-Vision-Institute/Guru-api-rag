# Guru RAG runtime configuration — TEMPLATE ONLY. Real values live in AWS Secrets Manager (main account, secret
# `guru-rag/env`, one JSON object) and are loaded at boot by guru-env.service into /run/guru/env. Never commit values.

# LLM providers. LLM_PROVIDERS is a comma list of enabled providers (openai, claude, gemini); staging runs OpenAI only.
LLM_PROVIDERS=openai
DEFAULT_MODEL=openai
OPENAI_API_KEY=
OPENAI_CHAT_MODEL=gpt-4o-mini
# GOOGLE_API_KEY=            # disabled; add the key and "gemini" to LLM_PROVIDERS to re-enable
# ANTHROPIC_API_KEY=         # disabled; add the key and "claude" to LLM_PROVIDERS to re-enable

# OpenSearch (VPC domain). Credentials are NOT hardcoded: utils.py/ingest.py read these.
OPENSEARCH_ENDPOINT=vpc-....us-east-1.es.amazonaws.com
OPENSEARCH_INDEX=guru-rag
OPENSEARCH_USER=admin
OPENSEARCH_PASSWORD=
REGION=us-east-1
EMBED_MODEL=text-embedding-3-large
EMBED_DIM=3072

# Access control (main.py middleware)
GURU_ADMIN_KEY=              # X-Guru-Key for /logs /docs /ingest-website /embed (else 404)
GURU_READONLY_KEYS=          # comma list; read-only identities for /ask /search (e.g. the LACS consumer)
WEBHOOK_SECRET=              # Vapi x-vapi-secret header value (assistant + bookAppointment tool)
GURU_ENFORCE_TOOL_SECRET=true

# Vapi (server-side private key; the browser widget uses the separate public key)
VAPI_API_KEY=
VAPI_ASSISTANT_ID=
BACKEND_API_URL=
WEBHOOK_URL=

# LACS approved-Q&A consumer (docs/operations/APPROVED-QA-CONSUMERS.md in KVI-LACS-Core)
LACS_CONSUMER_ENABLED=true
LACS_CONSUMER_DELIVERY=shadow   # off | shadow | live
LACS_ORIGIN=https://staging.khannainstitute.com
LACS_TOKEN_URL=https://auth.staging.khannainstitute.com/realms/kvi-staging/protocol/openid-connect/token
LACS_CLIENT_ID=kvi-lacs-staging-guru-consumer
LACS_CLIENT_SECRET=

# Approved public education (operator cutover after LACS public-resolve deployment)
# Set LACS_CONSUMER_DELIVERY=approved-public to activate exact approved wording.
# Keep approved-public and set LACS_CONSUMER_ENABLED=false for handoff-only rollback.
LACS_VOICE_PUBLIC_ENABLED=false
LACS_VOICE_PUBLIC_KEY=

