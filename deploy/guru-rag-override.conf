# /etc/systemd/system/guru-rag.service.d/override.conf
# Runtime secrets come from AWS Secrets Manager via guru-env.service, not from a .env file in the app directory.
[Unit]
Requires=guru-env.service
After=guru-env.service

[Service]
EnvironmentFile=/run/guru/env
Environment=AWS_REGION=us-east-1
